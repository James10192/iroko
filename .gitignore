# Dependencies & build output
node_modules/
dist/
pnpm-lock.yaml
*.lockb

# Personal Claude Code settings — never commit (template lives in templates/settings.json.tpl)
/settings.json
settings.local.json
